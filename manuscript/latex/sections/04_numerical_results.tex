\section{Числовий аналіз і обговорення результатів}
\label{sec:numerical-results}

Числовий аналіз виконано для нормованих параметрів
$\,d_i/l$, $\delta_i'$ і $J_i'$, визначених у
розділі~\ref{sec:derivation}. На відміну від попередньої редакції
рукопису, наведені нижче графіки побудовано за повторно перевіреними
розрахунками. Фізично допустимими вважаємо лише ті комбінації матеріалу,
кута та знака навантаження, для яких одночасно виконуються умови
\begin{equation*}
  Cg_2<0,\qquad CQ_i>0.
\end{equation*}
Перша нерівність забезпечує стискання берегів міжфазної зсувної тріщини,
а друга --- відривне нормальне напруження на бісектрисі матеріалу, в якому
утворюється зона передруйнування.

Для базової пари матеріалів
\begin{equation*}
  E_1/E_2=0.5,\qquad \nu_1=\nu_2=0.3,
\end{equation*}
зміна знака множника $g_2$ відбувається при
\begin{equation*}
  \alpha_1=12.9202^\circ,\qquad
  \alpha_2=107.0897^\circ.
\end{equation*}
Ці значення отримано безпосереднім розв'язуванням умов
$D_0(-1-\lambda_0)=0$ і $g_2=0$ та незалежно підтверджено реалізаціями
у Python і MATLAB. За $C>0$ зона передруйнування є фізично допустимою в
матеріалі~2 при $0<\alpha<\alpha_1$ і в матеріалі~1 при
$\alpha_2<\alpha<180^\circ$. За $C<0$ відповідні області мають вигляд
$\alpha_1<\alpha<90^\circ$ для матеріалу~1 та
$90^\circ<\alpha<\alpha_2$ для матеріалу~2. Значення
$\alpha=90^\circ$ відповідає прямолінійній межі поділу і надалі
розглядається як вироджена аналітична границя, а не як звичайна внутрішня
точка числової гілки.

Для оцінки впливу пружного контрасту ті самі два переходи обчислено для
низки значень $E_1/E_2<1$ при $\nu_1=\nu_2=0.3$. Результати наведено в
табл.~\ref{tab:g2-transition-angles}. Вони підтверджують авторську
таблицю в межах її початкової точності, за винятком округлення
$\alpha_1$ при $E_1/E_2=0.10$: точне значення $7.0299^\circ$ відповідає
$7.0^\circ$, а не $7.1^\circ$ при округленні до десятих.

\input{tables/table1_transition_angles.tex}

\subsection{Залежність від нормованого навантаження}
\label{subsec:figure2}

На рис.~\ref{fig:load-dependence} наведено залежності параметрів зони
передруйнування від
\begin{equation*}
  \sigma'=\frac{Cl^{\lambda_0}}{\sigma_i}
\end{equation*}
для чотирьох контрольних конфігурацій:
$\alpha=10^\circ$ ($i=2$, $C>0$),
$45^\circ$ ($i=1$, $C<0$),
$105^\circ$ ($i=2$, $C<0$) і
$135^\circ$ ($i=1$, $C>0$). Для кожної конфігурації показано лише
фізично допустиму піввісь навантаження.

\resultfigure{figure2_recalculated.pdf}{%
Залежність нормованих параметрів зони передруйнування від
безрозмірного навантаження $\sigma'$ для чотирьох контрольних кутів і
фізично допустимих матеріалів.}{fig:load-dependence}

У всіх чотирьох випадках $d_i/l$, $\delta_i'$ і $J_i'$ монотонно
зростають зі збільшенням $|\sigma'|$. Така синхронність безпосередньо
випливає з аналітичних співвідношень
$\delta_i'\propto d_i/l$ і $J_i'\propto d_i/l$ при фіксованій
геометрії. Серед наведених контрольних конфігурацій найбільші значення
усіх трьох параметрів при $|\sigma'|=0.5$ отримано для
$\alpha=45^\circ$, $C<0$; зокрема,
\begin{equation*}
  \frac{d_1}{l}=0.14743436,\qquad
  \delta_1'=0.14544227,\qquad
  J_1'=0.03716462.
\end{equation*}
Цей результат важливий також для перевірки маломасштабного припущення:
збільшення навантаження одночасно підсилює рушійну силу відгалуження і
наближає модель до межі її асимптотичної застосовності.

Виправлена крива для $\alpha=10^\circ$ є окремим контрольним випадком:
для $\sigma'=0.5$ незалежний авторський Mathcad-розрахунок і повторний
MATLAB-розрахунок дають практично однакові значення параметрів зони.
Таким чином, попередня розбіжність цієї кривої була наслідком помилкового
матеріального індексу в одному з плюс-факторів, а не властивістю
аналітичної моделі.

У новій авторській редакції запропоновано інтерпретувати графіки
$\delta_i'$ і $J_i'$ також як основу відповідно деформаційного та
енергетичного критеріїв зародження відгалуженої тріщини.

\AuthorQuery{AQ-NR-01}{Чи Ви справді хочете поряд з енергетичним критерієм
$J_i=J_{ic}$ ввести також деформаційний критерій
$\delta_i=\delta_{ic}$? Якщо так, потрібно пояснити фізичний зміст
$\delta_{ic}$, спосіб його визначення та дати відповідне посилання. Якщо
такого незалежного матеріального параметра ми не використовуємо,
пропонуємо залишити в статті лише енергетичний критерій.}

\subsection{Залежність від кута зламу межі поділу}
\label{subsec:figure3}

Рис.~\ref{fig:angle-dependence} показує залежність нормованих параметрів
зони від півкута $\alpha$ для тієї самої базової пари матеріалів при
$\sigma'=\pm0.5$. На рисунку залишено лише комбінації матеріалу та знака
навантаження, що задовольняють умовам фізичної допустимості. Переходи між
допустимими гілками відбуваються при $\alpha_1$,
$90^\circ$ і $\alpha_2$.

\resultfigure{figure3_recalculated.pdf}{%
Залежність нормованих параметрів зони передруйнування від півкута
$\alpha$ при $\sigma'=\pm0.5$. Показано лише фізично допустимі комбінації
матеріалу та знака навантаження; $\alpha=90^\circ$ є аналітичною межею.}%
{fig:angle-dependence}

У кожній основній допустимій області кутова залежність має виражений
максимум. Оскільки при фіксованому $\alpha$ величини $\delta_i'$ і
$J_i'$ пропорційні $d_i/l$, максимуми трьох параметрів є узгодженими.
Фізично це означає, що геометрія ламаної межі істотно впливає не на
матеріальну тріщиностійкість $J_{ic}$, а на величину рушійної сили
відгалуження, яка досягається за заданого зовнішнього навантаження.
Отже, за фіксованого $J_{ic}$ кути поблизу максимуму відповідають
несприятливішим геометричним конфігураціям і нижчому критичному
зовнішньому навантаженню.

\AuthorQuery{AQ-NR-02}{У новій редакції максимум $J_i$ при певних кутах
інтерпретовано як ``найменшу тріщиностійкість з'єднання''. Чи Ви маєте на
увазі не зміну матеріальної тріщиностійкості $J_{ic}$, а найменше
критичне зовнішнє навантаження, необхідне для ініціювання відгалуження за
фіксованого $J_{ic}$? Ми пропонуємо саме так сформулювати цей результат.}

Максимум синьої гілки на рис.~\ref{fig:angle-dependence} відповідає
$d_i/l$ порядку $0.18$. Це значення вже не є беззастережно малим порівняно
з одиницею, тоді як побудова локальної задачі ґрунтується на припущенні
$d_i/l\ll1$. Тому повна крива корисна для аналізу структури розв'язку,
але кількісну інтерпретацію області поблизу найбільшого максимуму слід
проводити з урахуванням цієї межі застосовності.

\AuthorQuery{AQ-NR-03}{На рис.~3(a) максимальне значення $d_i/l$ сягає
приблизно $0.18$, тоді як локальна постановка ґрунтується на припущенні
$d_i\ll l$. Як Ви пропонуєте трактувати цю область? Ми схиляємося до
того, щоб залишити повну математичну криву, але прямо зазначити, що
поблизу максимуму кількісна точність маломасштабної моделі потребує
обережності, і візуально або текстово позначити область послаблення
припущення $d_i/l\ll1$.}

Числове обчислення плюс-факторів для рис.~\ref{fig:angle-dependence}
потребує особливої уваги при $\alpha\to0$ та
$\alpha\to180^\circ$. Фіксований контур факторизації, використаний у
початковому авторському розрахунку, повільно збігається біля цих меж.
У повторному розрахунку застосовано адаптивне розширення контуру; при
додатковому збільшенні хвоста максимальна зміна перевірених
плюс-факторів становила $8.69\times10^{-13}$. Тому остаточний
рис.~\ref{fig:angle-dependence} побудовано за збіжними адаптивними
значеннями.

\subsection{Показники сингулярності та фізична допустимість}
\label{subsec:figure4}

Поява міжфазної зсувної тріщини та подальше формування зони
передруйнування послідовно змінюють сингулярність поля напружень у
кутовій точці. Для базової пари матеріалів ці три стадії описуються
показниками $\lambda_0$, $\lambda$ та $\lambda_i$ відповідно.

\resultfigure{figure4_admissible_segments.pdf}{%
Показники сингулярності та фізично допустимі сегменти для базової пари
матеріалів. Повні математичні гілки та фізично допустимі ділянки
розрізняються відповідно до умов $Cg_2<0$ і $CQ_i>0$.}%
{fig:singularity-exponents}

У межах фізично допустимих сегментів для дослідженої базової пари
матеріалів утворення міжфазної зсувної тріщини істотно підсилює
сингулярність: модуль $\lambda$ перевищує модуль показника інтактної
кутової точки $\lambda_0$. Подальша поява зони передруйнування частково
послаблює цю концентрацію, причому для відповідного матеріалу виконується
співвідношення
\begin{equation*}
  |\lambda_0|<|\lambda_i|<|\lambda|.
\end{equation*}
Отже, зона передруйнування виконує локальну екранувальну роль: вона
знижує порядок сингулярності, спричинений міжфазною зсувною тріщиною,
але не усуває концентрацію повністю. Це співвідношення тут
встановлюється для розглянутої пари матеріалів і фізично допустимих
гілок; його не слід без додаткової перевірки переносити на довільні
пружні контрасти.

Підсилення сингулярності після появи міжфазної тріщини створює передумови
для локального руйнування одного з матеріалів, однак саме існування зони
визначається не лише величиною показника сингулярності, а й умовами
$Cg_2<0$, $CQ_i>0$, опором відриву $\sigma_i$ та рівнем зовнішнього
навантаження. Така інтерпретація пов'язує послідовність
$\lambda_0\to\lambda\to\lambda_i$ із механічною моделлю початкової стадії
відгалуження, не надаючи самій сингулярності статусу достатнього критерію
руйнування.